name: Call workflow
permissions:
  contents: read
  id-token: write
on:
  workflow_dispatch:
jobs:
  Call_reusable_workflow:
      uses: mevijays/vijay-work/.github/workflows/build.yaml@main
      with:
        java_version: 8
