name: testing
on:
  workflow_dispatch:
    inputs:
      TESTENV:
        default: "false"
        required: true
        type: choice
        options:
         - "dev"
         - "prod"
env:
  REGISTRY_URL: reg.mevijay.dev
  ENVIRONMENT: ${{ github.event.inputs.TESTENV }}
jobs:
  docker-dev:
    if: ${{ github.event.inputs.TESTENV }} == 'dev' 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: echo "hello DEVELOPESS!"
  docker-prod:
    if: ${{ github.event.inputs.TESTENV }} == 'prod' 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        run: echo "hello PRODUCTION!"
  
