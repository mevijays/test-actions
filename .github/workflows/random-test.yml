name: random temp
permissions:
  contents: 'read'
  id-token: 'write'
on:
  workflow_dispatch:
jobs:
  example-test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v3
    - name: testing echo
      run: |
        echo '================================'
        echo ${GITHUB_SHA::6}
        echo '================================'
  
